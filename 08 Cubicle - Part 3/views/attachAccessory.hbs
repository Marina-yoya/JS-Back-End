<h1>Attach a new accessory</h1>
<div class='form details'>
  <h2>{{cube.name}}</h2>
  <img class='cube' src='{{cube.imageUrl}}' />
  {{#if accessories}}
    <form action='/products/attach/{{cube._id}}' method='POST'>
      <label for='accessories'>Accessories</label>
      <select id='accessories' name='accessory' required=''>
        <option value='' disabled='' selected=''>Chose accessory</option>
        {{#each accessories}}
          <option value='{{_id}}'>{{name}}</option>
        {{/each}}
      </select>
      <input class='btn' type='submit' value='Attach' />
    </form>
  {{else}}
    <h3 class='italic'>This cube has all available accessories so far or there is no available at all...</h3>
  {{/if}}
  <a class='btn' href='/products/details/{{cube._id}}'>Back</a>
</div>